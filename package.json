{
    "name": "remote.performance.timing",
    "version": "0.0.1",
    "description": "Call a tracking pixel with the info about timing of the various events' that happen during the webpage loading.",
    "contributors": [
        "Alexander Jäger <alexander.jaeger@me.com>"
    ],
    "main": "./lib/remote.performance.timing",
    "directories": {
        "lib": "./lib"
    },
    "engines": {
        "node": "*"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "grunt": "~0.4.2",
        "grunt-contrib-uglify": "~0.2.2"
    }
}
